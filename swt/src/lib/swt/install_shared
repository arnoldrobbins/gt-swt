# install_shared --- install shared Subsystem library

   declare _search_rule = "^int,^var,=bin=/&"

   cp vswtlb =newlib=
   cp sw[cat segment] =newsystem=
   cp inst =newsystem=/sw4000

   chat =newlib=/vswtlb
