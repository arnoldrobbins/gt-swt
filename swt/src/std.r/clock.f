      LOGICAL FLAG
      LOGICAL TQUIT$
      INTEGER TIME(9),PREV
      INTEGER BACKUP(9)
      DATA BACKUP/8*136,0/
      CALL BREAK$(1)
      PREV=160
10000   CONTINUE
10001     CALL DATE(2,TIME)
          IF((TIME(8).EQ.PREV))GOTO 10002
            GOTO 10003
10002     CALL SLEEP$(INTL(100))
        GOTO 10001
10003   PREV=TIME(8)
        CALL PUTLIN(TIME,-15)
        CALL SLEEP$(INTL(900))
        IF((.NOT.TQUIT$(FLAG)))GOTO 10004
          GOTO 10005
10004   CALL PUTLIN(BACKUP,-15)
      GOTO 10000
10005 CALL BREAK$(0)
      CALL SWT
      END
C ---- Long Name Map ----
